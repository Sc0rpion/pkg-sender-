#!/bin/sh
# Check if python3 is available
if ! which python3 >/dev/null 2>&1 && [ ! -x "/usr/local/bin/python3" ] && [ ! -x "/usr/bin/python3" ]; then
    echo "Python 3 is required. Please install Python 3 package from Synology Package Center first." >&2
    exit 1
fi
exit 0
